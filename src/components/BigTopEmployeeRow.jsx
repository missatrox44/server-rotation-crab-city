import React from 'react';
import ReadyBtn from "./Btns/ReadyBtn";

function BigTopEmployeeRow({employee, setEmployees, lastAction, bigTopEmployees}) {

  const handleReady = (employee) => {
    // function body
  };

  return (
    <tr key={employee.id}>
      {/* rendering logic */}
    </tr>
  );
}

export default BigTopEmployeeRow;
